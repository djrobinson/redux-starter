import React from 'react';
import PropTypes from 'prop-types';

const JsxStub = (props) => {
    return (
        <div>
            {}
        </div>
    );
};

JsxStub.propTypes = {
    children: PropTypes.string
}

export default JsxStub;